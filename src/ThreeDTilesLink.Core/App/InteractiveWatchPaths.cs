namespace ThreeDTilesLink.Core.App
{
    internal static class InteractiveWatchPaths
    {
        internal const string BasePath = "World/ThreeDTilesLink";
        internal const string LatitudePath = $"{BasePath}.Latitude";
        internal const string LongitudePath = $"{BasePath}.Longitude";
        internal const string RangePath = $"{BasePath}.Range";
        internal const string SearchPath = $"{BasePath}.Search";
    }
}
